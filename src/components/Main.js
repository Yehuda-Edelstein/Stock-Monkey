import React from 'react';
import Header from './Header';
import Form from './Form.jsx';

function Main(props) {
    return (
        <div>
            <Header />
            <Form />
        </div>
    );
}

export default Main;